# devops-1
🚀 Краткое описание проекта
Данный проект представляет собой одностраничное React-приложение (SPA) для управления задачами, которое упаковывается в Docker-контейнер для простоты развертывания и масштабирования.

💡 Цель проекта: Продемонстрировать практики контейнеризации фронтенд-приложений и обеспечить идентичную среду выполнения на всех этапах разработки и продакшена.

🎯 Основные цели:
Создать отзывчивый React-интерфейс

Обеспечить изолированную среду выполнения через Docker

Упростить процесс развертывания

Подготовить приложение для CI/CD пайплайнов

📋 Требования к среде
Компонент	Версия	Назначение
Node.js	18.x или выше	Среда выполнения для сборки React-приложения
npm	9.x или выше	Менеджер пакетов для установки зависимостей
Docker	20.x или выше	Платформа для контейнеризации приложения
Docker Compose	2.x или выше	Оркестрация многоконтейнерных приложений
⚠️ Важно: Убедитесь, что Docker Engine запущен перед сборкой и запуском контейнеров.
🛠️ Установка и запуск
Локальная разработка (без Docker)
# Клонирование репозитория
git clone https://github.com/your-username/react-docker-app.git
cd react-docker-app

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm start
Сборка и запуск через Docker
# Dockerfile
FROM node:18-alpine as build

WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/build /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
Команды для сборки и запуска:
# Сборка Docker-образа
docker build -t react-docker-app .

# Запуск контейнера
docker run -d -p 3000:80 --name react-app react-docker-app

# Или с помощью Docker Compose
docker-compose up -d
📁 Структура проекта
react-docker-app/
├── public/
│   ├── index.html
│   └── favicon.ico
├── src/
│   ├── components/
│   │   ├── TaskList.js
│   │   ├── TaskForm.js
│   │   └── Header.js
│   ├── styles/
│   │   └── App.css
│   ├── App.js
│   └── index.js
├── Dockerfile
├── docker-compose.yml
├── nginx.conf
├── package.json
└── README.md
